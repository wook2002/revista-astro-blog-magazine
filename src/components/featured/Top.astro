---
import slugify from 'slugify';
import { getColor } from '../../utils/heroCategories';
import { resolveImagePath } from '../../utils/imagePath';
import { normalizePostData, getCategoryUrl } from '../../utils/postHelpers';
import { formatDateShort } from '../../utils/formatDate';
import type { Post } from '../../utils/getPosts';
// 1. 설정 파일 불러오기
import { SITE_INFO } from '../../constants/siteConfig';

interface Props {
  posts?: Post[];
}

const { posts = [] } = Astro.props;
const topPost = posts[0];
---

<div class="col-md-4 col-12">
  {/* 2. 섹션 제목 연결 */}
  <h2 class="section-title">{SITE_INFO.featured.topTitle}</h2>

  {topPost ? (() => {
    const normalized = normalizePostData(topPost);
    const imageSrc = resolveImagePath(topPost.data.image);
    const categoryColor = getColor(topPost.data.category);

    return (
      <article key={topPost.id} class="card card-bg card-shadow">
        <a href={normalized.url}>
          {imageSrc && (
            <img
              src={imageSrc}
              alt={normalized.title}
              class="img"
              style="width: 100%; height: auto;"
            />
          )}
        </a>

        <div class="card-body">
          <a href={normalized.url}>
            <h3>{normalized.title}</h3>
          </a>

          <ul class="card-meta list-inline">
            <li class="list-inline-item">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"></circle>
                <polyline points="12 6 12 12 16 14"></polyline>
              </svg>
              {/* 3. 읽기 시간 단위 연결 */}
              {normalized.readingTime} {SITE_INFO.posts.readingTime}
            </li>
            <li class="list-inline-item">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                <line x1="16" y1="2" x2="16" y2="6"></line>
                <line x1="8" y1="2" x2="8" y2="6"></line>
                <line x1="3" y1="10" x2="21" y2="10"></line>
              </svg>
              {normalized.dateShort}
            </li>
            <li class="list-inline-item">
              <a
                href={normalized.categoryUrl}
                class="category-link"
                style={`color: ${categoryColor}; background-color: ${categoryColor};`}
              >
                <div class="category-text-color">{normalized.category}</div>
              </a>
            </li>
          </ul>
          <a href={normalized.url}>
            <p class="excerpt">{normalized.excerpt}</p>
          </a>
          <a class="btn btn-outline-primary" href={normalized.url}>
            {/* 4. 더 보기 버튼 연결 */}
            {SITE_INFO.recentPosts.readMore}
          </a>
        </div>
      </article>
    );
  })() : null}
</div>
