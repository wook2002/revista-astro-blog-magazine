---
// ... 상단 import 동일 ...
---

<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <title>{pageTitle}</title>
    
    <meta name="robots" content="noindex, nofollow" />
    <meta name="googlebot" content="noindex, nofollow" />

    {/* 💡 아까 넣었던 <style is:inline>... 은 시원하게 지워버리세요! */}
    
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    {/* ... 폰트 및 OG 태그 동일 ... */}
  </head>
  <body>
    {/* 🚀 client:load 대신 client:only="react"를 씁니다. */}
    {/* 서버는 이 자리를 비워두고, 브라우저가 리액트로 직접 그리게 합니다. */}
    <NavbarWrapper client:only="react" />
    
    <main>
      <slot />
    </main>
    <Footer />

    {/* 💡 하단에 넣었던 <script is:inline>... 도 싹 지우세요! */}
  </body>
</html>
